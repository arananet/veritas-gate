---
title: A project check override keeps the profile fields it does not set
slug: check-override-merge
type: bugfix
status: review
author: Claude
created_at: '2026-09-26'
updated_at: '2026-09-26'
roles:
  implementer: '@arananet'
  reviewer: '@arananet'
  qa: '@arananet'
  product_owner: '@arananet'
description: |
  Setting only `target` for the profile's `arxiv-package` check in veritas.yaml
  replaced the whole check, so `type` fell back to its default and the check
  failed as "defines no command". A project check with the name of a profile
  check now overrides only the fields the project wrote.
acceptance_criteria:
- "Given a project check naming a profile check with only target set, when checks are merged, then type and severity come from the profile and target from the project."
- "Given a project check with a new name, when checks are merged, then it is used as written."
test_plan:
- 'tests/test_engine.py covers the merge.'
qa_notes: ''
out_of_scope: []
implementation_skill:
eval_plan:
  scenarios: []
  metrics: []
technical_notes: 'Uses pydantic exclude_unset on the override.'
